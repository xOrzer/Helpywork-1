<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helpywork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProposerEnLigne" />
        <activity android:name=".ProposerDomicile" />
        <activity android:name=".ListePostes" />
        <activity android:name=".AideEnLigne" />
        <activity android:name=".ProposerAide" />
        <activity android:name=".AideDomicile" />
        <activity android:name=".BesoinAide" />
        <activity android:name=".Account" />
        <activity android:name=".Login"/>

        <activity android:name=".CreateAccount" />
    </application>

</manifest>